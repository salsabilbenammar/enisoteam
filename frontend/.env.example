# Laisser vide en local (proxy Vite) ou si même domaine que l’API.
#
# Production sans carte (Vercel + Render) — OBLIGATOIRE au build :
# VITE_API_URL=https://TON-SERVICE.onrender.com/api
VITE_API_URL=
