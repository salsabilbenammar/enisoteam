# Laisser vide si le site et l’API sont sur le même domaine
# (Nginx/Caddy proxifient /api et /uploads, ou Express sert frontend/dist).
#
# Si l’API est sur un autre domaine, définir au build, ex. :
# VITE_API_URL=https://api.votredomaine.com/api
VITE_API_URL=
